{
  "name": "clarity-reel-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Reel worker: Instagram links -> transcript + recipe/synopsis. MODE=process drains the queue as reels are dumped; MODE=digest sends one 4am email. Runs on GitHub Actions, isolated from the Vite app.",
  "scripts": {
    "start": "node process-queue.mjs",
    "lab": "node --env-file=.env lab/server.mjs",
    "test": "node run-tests.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.6",
    "ffmpeg-static": "^5.2.0"
  }
}
